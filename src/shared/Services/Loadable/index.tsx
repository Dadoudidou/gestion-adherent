import * as React from "react";
/*
export class Loading extends React.PureComponent<any, any>
{
    render(){
        return (<div>Chargement ...</div>)
    }
}

import * as LoadableFN from "react-loadable"

type loadableProps = {
    loading?: React.ComponentType<LoadableFN.LoadingComponentProps> | (() => null);
    delay?: number | false | null;
    timeout?: number | false | null;
    webpack?: () => number[];
    modules?: string[];
    render?: (loaded: {[key: string]: any}, props: any) => React.ReactNode;

    loader: () => Promise<{[key: string]: any}> | Promise<React.ComponentType<any> | { default: React.ComponentType<any> }> | { [key: string]: () => Promise<any> };
}

export const loadable = (props: loadableProps): React.ComponentType<any> & LoadableFN.LoadableComponent => {
    return LoadableFN({
        loading: Loading,
        ...props,
    } as any)
}*/

export const test = "";